export type Product = {
  description: string;
  photoPath: string;
  price: number;
  stock: number;
  title: string;
  catalogName: string;
  Quantity: number;
  id: number;
};

export type ProductsParams = {
  id: string;
};
